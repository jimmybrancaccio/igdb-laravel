{
    "name": "marcreichel/igdb-laravel",
    "description": "A Laravel wrapper for version 4 of the IGDB API (Apicalypse) including webhook handling",
    "keywords": [
        "laravel",
        "api-wrapper",
        "igdb",
        "igdb-api",
        "apicalypse",
        "wrapper"
    ],
    "type": "library",
    "minimum-stability": "stable",
    "require": {
        "php": "^8.4",
        "ext-json": "*",
        "illuminate/support": "^12.0|^13.0",
        "guzzlehttp/guzzle": "~6.0|~7.0",
        "nesbot/carbon": "^2.53.1|^3.0"
    },
    "require-dev": {
        "orchestra/testbench": "^10.0|^11.0",
        "nunomaduro/collision": "^8.9.4",
        "roave/security-advisories": "dev-latest",
        "larastan/larastan": "^3.10.0",
        "laravel/pint": "^1.29.1",
        "pestphp/pest": "^4.7.2",
        "pestphp/pest-plugin-type-coverage": "^4.0.4",
        "rector/rector": "^2.4.5"
    },
    "license": "MIT",
    "authors": [
        {
            "name": "Marc Reichel",
            "email": "mail@marcreichel.de"
        }
    ],
    "scripts": {
        "pint": "./vendor/bin/pint --test -v",
        "test": "./vendor/bin/pest --parallel --no-coverage",
        "stan": "./vendor/bin/phpstan --memory-limit=2G",
        "test:coverage": [
            "@putenv XDEBUG_MODE=coverage",
            "./vendor/bin/pest --parallel --coverage --min=90 --coverage-clover build/clover.xml"
        ],
        "test:coverage-html": [
            "@putenv XDEBUG_MODE=coverage",
            "./vendor/bin/pest --parallel --coverage --min=90 --coverage-html build/coverage"
        ],
        "test:type-coverage": [
            "@putenv XDEBUG_MODE=coverage",
            "php -d memory_limit=2G ./vendor/bin/pest --parallel --type-coverage --min=100"
        ]
    },
    "autoload": {
        "psr-4": {
            "MarcReichel\\IGDBLaravel\\": "src"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "MarcReichel\\IGDBLaravel\\Tests\\": "tests"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "MarcReichel\\IGDBLaravel\\IGDBLaravelServiceProvider"
            ]
        }
    },
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    }
}
